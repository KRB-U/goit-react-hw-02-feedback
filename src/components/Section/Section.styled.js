import styled from 'styled-components';

const DivSectionTitle = styled.div`
  /* display: flex;
  align-items: center; */
`;

const HTitle = styled.h2`
  font-size: 25px;
`;

export { DivSectionTitle, HTitle };
